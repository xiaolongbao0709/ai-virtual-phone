# 小手机 (ai-virtual-phone) 项目记忆书

> 生成时间：2026-08-06  
> 用途：本文件包含项目的完整上下文，切换对话窗口时可直接携带  
> 用法：新窗口中说「请先阅读 项目记忆书.md」即可恢复完整上下文

---

## 一、项目身份

| 项 | 值 |
|---|-----|
| 仓库 | `zhaozhao0326/ai-virtual-phone` |
| 本地目录 | `C:\Users\乖乖\WorkBuddy\2026-08-04-08-56-47\ai-virtual-phone-new` |
| 部署地址 | `https://ai-virtual-phone-taupe-xi.vercel.app` |
| 分支 | `main` |
| 技术栈 | Next.js 15 + React 19 + Vercel + Supabase |
| 部署方式 | `git push origin main` → Vercel 自动构建 |

---

## 二、已完成功能清单

### 生图系统
- **NovelAI (NAI)**：Vercel 服务器中转 `image.novelai.net`，v11 验证通过
  - 支持 v4.5 模型、中文自动翻译、ZIP 解压、并发锁自动重试(b1fd7ff)
  - 锁脸：`character_reference` + fidelity 0.75，前 4 张参考图
  - NSFW 开关(f3d0bd0)、风格预设 6 组(a48033e)
  - 实时状态显示(8569985)：生成中 / 等锁中 / 重试中
- **OpenAI 兼容 (OAI)**：永远走 Vercel 中转(v13, 42f7994)，不再跨境
  - 结构化提示词 + 参考图锁脸(v19)
  - 无法 NSFW（OpenAI 服务端强制）
- **Pollinations**：免费免 Key，浏览器直连，限流严格

### 朋友圈 (Moments)
- U 发帖 + 角色随机评论/点赞 + NPC 互动
- 「照片里有谁」人标签(9dc6687) + 加权评论概率(1e4cf2d)
- LLM 429 自动重试(d6d1af3)：35s→65s→95s
- 图片失效修复(124a8b7)：显示"图片已失效"+重新生成入口

### 角色相册 (C 独立相册)
- LLM 驱动自发拍照：[相册] action + NAI 锁脸生图 + C 第一视角随想
- 存储：character-album-store.ts (IndexedDB)，按 characterId 隔离
- 入口：me 主页显眼「角色相册」区块(f741430) + 朋友圈互动设置双入口
- 使用要求：角色需开启"自动发帖"才触发 [相册] 标记

### 世界书兼容
- 兼容 SillyTavern 世界书 JSON（entries 字典/数组，uid/key/constant/order）
- 兼容 SillyTavern 人设卡 V2/V3 格式
- 文件：settings-storage.ts + character-storage.ts

### AI 桌面助手「小卷」
- 10 套件 / 51 工具，支持文本协议 `[执行动作:工具名(...)]` 和原生 function calling
- 套件清单：生图/世界书/角色管理/消息/朋友圈/图像处理/世界关系网/聊天控制/记忆/预设
- 角色卡套件：4 工具（读/创/改字段/删）
- 导航能力：覆盖全部 28 个桌面页面 + me 个人中心子页面(相册/朋友圈互动/生图设置/高级工具)
- 记忆系统(4d9e49f)：三层模型(核心记忆/长期记忆/短期记忆)，5 个工具(搜索/写入/更新/删除/沉淀)
- 聊天控制：读取聊天/编辑消息/换头像/群聊剧本

### 其他
- 横屏适配(fa5e3ba)：手机横屏时缩放铺满可视高度
- PWA 缓存修复(v16, ac692cf)：JS 改为 network-first
- 用户锁脸参考图(d951bdf)：设置-User Identity 独立上传

---

## 三、小卷工具系统速查（10 套件 / 49 工具）

| 套件 | 工具数 | 核心能力 |
|------|--------|---------|
| image_gen_pack | 7 | NAI/OAI/Pollinations 生图、上传图床 |
| world_book_pack | 6 | 世界书词条第增删改查、导入/导出 |
| character_pack | 4 | 角色卡读/创/改字段/删 |
| message_pack | 5 | 发消息、群发、撤回、消息模板 |
| moments_pack | 6 | 发朋友圈、评论、点赞、时间线 |
| image_utils_pack | 6 | 图片裁剪/滤镜/拼接/压缩/信息读取 |
| world_pack | 3 | 创建世界、导入角色、创建关系 |
| chat_pack | 4 | 读取聊天/编辑消息/换头像/群聊剧本 |
| memory_pack | 5 | 搜索/写入/更新/删除/沉淀记忆 |
| preset_pack | +4 | 预设管理（列表/读取/新建/更新词条） |
| navigate | 1 | 导航到任意桌面页面 |

---

## 四、重要技术教训

1. **GitHub 网页上传**：必须先点进目标文件夹，再点 Add file → Upload files，否则全落根目录
2. **git 沙箱限制**：push 必须用户手动执行；回退文件优先用 `git show HEAD:path > path` 而非 stash/checkout
3. **NAI 生图**：网页版必须走 Vercel 服务器中转（浏览器 CORS+跨境），域名用 `image.novelai.net`（非 api.novelai.net）
4. **PWA 缓存**：JS/CSS 换 network-first，否则前端修复永远不生效
5. **TypeScript**：改完代码务必 `tsc --noEmit` 验证；`Partial<T>` 避免 `{}` 推断
6. **Vercel/Supabase 国内访问**：间歇被墙，可能需要反代或 Cloudflare

---

## 五、用户偏好

- 生图：OAI 画风好但审核严；NAI 可 NSFW 但画质不如 OAI
- 亲密/浪漫内容只能走 NAI（OAI 会拦）
- GitHub 操作需要极具体的分步指引（点哪个文件夹、拖哪个文件）
- 部署习惯：代码改完后用户手动 push 或网页上传

---

## 六、当前状态与待办

### 代码已推 main，待 Vercel 部署生效
- 记忆系统 memory_pack (4d9e49f) ← 最新 commit

### 已知问题
- NAI 单账号同时间只跑 1 张图（账号级限制），双开标签页会撞锁
- Vercel + Supabase 国内需梯子/改 DNS
- 角色相册入口在 me 主页，非独立主屏图标

### 用户曾提但未完成的需求
- ~~导演对讲机（inject_narrator_event）~~ ✅ 已实现 (e911861)
- ~~lore 自动注入（extract_and_inject_lore）~~ ✅ 已实现 (e911861)
- ~~hiphone 联动~~ ❌ 已删除——功能无实际用途，对外开放接口纯属多余
